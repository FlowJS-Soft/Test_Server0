import { MatchMakerDriver, QueryHelpers, RoomListingData } from './Driver';
export declare class MongooseDriver implements MatchMakerDriver {
    constructor(connectionURI?: string);
    createInstance(initialValues?: any): RoomListingData;
    find(conditions: any, additionalProjectionFields?: {}): Promise<RoomListingData[]>;
    findOne(conditions: any): QueryHelpers<RoomListingData>;
}
