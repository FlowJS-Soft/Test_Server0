/// <reference types="node" />
import WebSocket from 'ws';
import { Client } from '..';
import { MatchMaker } from '../MatchMaker';
import { ServerOptions } from './../Server';
import { Transport } from './Transport';
export declare class WebSocketTransport extends Transport {
    protected wss: WebSocket.Server;
    protected pingInterval: NodeJS.Timer;
    protected pingTimeout: number;
    protected pingCountMax: number;
    constructor(matchMaker: MatchMaker, options: ServerOptions, engine: any);
    listen(port: number, hostname?: string, backlog?: number, listeningListener?: Function): this;
    shutdown(): void;
    protected autoTerminateUnresponsiveClients(pingTimeout: number, pingCountMax: number): void;
    protected onConnection: (client: Client, req?: any) => Promise<void>;
}
